export const ROUTE_PATH = Object.freeze({
  BASE: '/',
  LOGIN: '/login',
});

export const API_PATH = Object.freeze({
  BASE: '/api',

  PRODUCTS: {
    ALL: '/product/products',
  },

  USER: {
    BY_ID: '/user/:id',
  },

  AUTH: {
    TOKEN: '/auth',
  },
});
